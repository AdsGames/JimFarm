{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "detail": "Run and build jim farm",
      "windows": {
        "command": "make -j24 && cd build && JimFarm.exe"
      },
      "linux": {
        "command": "cmake --build . && cd build && ./JimFarm"
      },
      "group": "build",
      "problemMatcher": "$gcc"
    }
  ]
}
